{% extends 'base.html.twig' %}

{% block body %}
    <h1>Content</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ content.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ content.title }}</td>
            </tr>
            <tr>
                <th>Value</th>
                <td>{{ content.value }}</td>
            </tr>
            <tr>
                <th>block</th>
                <td>{{ content.block }}</td>
            </tr>
            <tr>
                <th>Value</th>
                <td>{{ content.order }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('content_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('content_edit', { 'id': content.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
